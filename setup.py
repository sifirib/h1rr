from setuptools import setup

setup(
   name='h1rr',
   version='0.1',
   description='A simple and strong wordlist creator',
   author='hrX',
   packages=['h1rr.py'],  
   install_requires=['python'],
)
